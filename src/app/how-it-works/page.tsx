import { ClientRedirect } from "@/components/util/client-redirect";

/** How It Works lives on the homepage as a section. */
export default function HowItWorksPage() {
  return <ClientRedirect href="/#how-it-works" />;
}
